<main class="success-container">
  <!-- Congratulations Modal/Card -->
  <div class="modal-overlay" id="congratsModal">
    <div class="congratulations-card">
      <div class="modal-header">
        <div class="confetti">🎉</div>
        <h1>Congratulations!</h1>
        <p class="subtitle">Your account has been created successfully</p>
      </div>

      <div class="modal-body">
        <div class="check-icon">✓</div>
        <h2 id="welcomeName">Welcome to CSE Motors!</h2>
        <p class="welcome-text">
          Your account is now active and ready to use. 
          <br>
          You can now browse our complete inventory, manage your account, and much more!
        </p>

        <div class="next-steps">
          <h3>What's Next?</h3>
          <ul>
            <li>✓ Browse our vehicle inventory</li>
            <li>✓ Save your favorite vehicles</li>
            <li>✓ Manage your account settings</li>
            <li>✓ View special offers and deals</li>
          </ul>
        </div>
      </div>

      <div class="modal-footer">
        <button class="btn-modal-primary" id="continueBtn">Continue to Dashboard</button>
        <p class="modal-note">You'll be automatically redirected in <span id="autoRedirect">10</span> seconds</p>
      </div>
    </div>
  </div>

  <!-- Main Success Page Content (hidden initially) -->
  <div class="success-card" id="successContent" style="display: none;">
    <!-- Success Icon -->
    <div class="success-icon">✓</div>

    <!-- Heading -->
    <h1>Account Created Successfully!</h1>

    <!-- Welcome Message -->
    <div class="welcome-message">
      <p>Welcome, <strong id="userDisplay">User</strong>!</p>
      <p>Your account has been created and you are now registered with CSE Motors.</p>
    </div>

    <!-- Account Details -->
    <div class="account-details-box">
      <h2>Account Information</h2>
      <div class="detail-item">
        <span class="label">Username:</span>
        <span class="value" id="displayUsername">---</span>
      </div>
      <div class="detail-item">
        <span class="label">Email:</span>
        <span class="value" id="displayEmail">---</span>
      </div>
    </div>

    <!-- Features Info -->
    <div class="features-box">
      <h2>What's Next?</h2>
      <ul>
        <li>✓ Browse our complete vehicle inventory</li>
        <li>✓ Save your favorite vehicles</li>
        <li>✓ Manage your account information</li>
        <li>✓ View your account details anytime</li>
      </ul>
    </div>

    <!-- Action Buttons -->
    <div class="action-buttons">
      <a href="/" class="btn btn-primary">Go to Home Page</a>
      <a href="/account/" class="btn btn-secondary">View My Account</a>
      <a href="/inv/" class="btn btn-secondary">Browse Inventory</a>
    </div>

    <!-- Auto Redirect Message -->
    <p class="redirect-message">
      If not redirected automatically, <a href="/">click here</a> to return to home page.
      <br>
      <small>Redirecting in <span id="countdown">5</span> seconds...</small>
    </p>
  </div>
</main>

<style>
  /* Modal Overlay Styles */
  .modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(0, 0, 0, 0.7);
    display: flex !important;
    align-items: center;
    justify-content: center;
    z-index: 1000;
    padding: 1rem;
    animation: fadeIn 0.3s ease-out;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  .congratulations-card {
    background: white;
    border-radius: 16px;
    padding: 3rem 2rem;
    max-width: 500px;
    width: 100%;
    box-shadow: 0 25px 80px rgba(0, 0, 0, 0.4);
    animation: slideUp 0.5s ease-out;
    position: relative;
    overflow: hidden;
  }

  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(50px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .modal-header {
    text-align: center;
    margin-bottom: 2rem;
  }

  .confetti {
    font-size: 60px;
    animation: bounce 0.8s ease-in-out infinite;
    display: inline-block;
  }

  @keyframes bounce {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  .modal-header h1 {
    font-size: 2.5rem;
    color: #4caf50;
    margin: 1rem 0 0.5rem 0;
    font-weight: 700;
  }

  .modal-header .subtitle {
    color: #666;
    font-size: 1.1rem;
    margin: 0;
  }

  .modal-body {
    text-align: center;
    margin-bottom: 2rem;
  }

  .check-icon {
    font-size: 80px;
    color: #4caf50;
    margin-bottom: 1rem;
    animation: scaleIn 0.6s ease-out;
  }

  @keyframes scaleIn {
    from {
      transform: scale(0);
    }
    to {
      transform: scale(1);
    }
  }

  .modal-body h2 {
    font-size: 1.8rem;
    color: #333;
    margin: 1rem 0;
  }

  .welcome-text {
    color: #666;
    font-size: 1rem;
    line-height: 1.6;
    margin-bottom: 2rem;
  }

  .next-steps {
    background-color: #f0f7ff;
    border-left: 4px solid #2196f3;
    padding: 1.5rem;
    border-radius: 8px;
    text-align: left;
    margin-bottom: 1.5rem;
  }

  .next-steps h3 {
    color: #1565c0;
    margin-top: 0;
    margin-bottom: 1rem;
    font-size: 1.1rem;
  }

  .next-steps ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .next-steps li {
    color: #333;
    padding: 0.5rem 0;
    font-size: 0.95rem;
  }

  .modal-footer {
    text-align: center;
    border-top: 1px solid #eee;
    padding-top: 1.5rem;
  }

  .btn-modal-primary {
    background-color: #4caf50;
    color: white;
    border: none;
    padding: 1rem 2rem;
    border-radius: 8px;
    font-size: 1.1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    width: 100%;
    margin-bottom: 1rem;
  }

  .btn-modal-primary:hover {
    background-color: #45a049;
    box-shadow: 0 4px 12px rgba(76, 175, 80, 0.3);
    transform: translateY(-2px);
  }

  .btn-modal-primary:active {
    transform: translateY(0);
  }

  .modal-note {
    color: #999;
    font-size: 0.85rem;
    margin: 0;
  }

  #autoRedirect {
    color: #4caf50;
    font-weight: bold;
  }

  /* Success Container Styles */
  .success-container {
    min-height: calc(100vh - 200px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  }

  .success-card {
    background: white;
    border-radius: 12px;
    padding: 3rem 2rem;
    max-width: 600px;
    width: 100%;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
    text-align: center;
    animation: slideUp 0.6s ease-out;
  }

  .success-icon {
    font-size: 80px;
    color: #4caf50;
    margin-bottom: 1rem;
    animation: scaleIn 0.8s ease-out;
  }

  .success-card h1 {
    color: #333;
    font-size: 2rem;
    margin-bottom: 1rem;
    margin-top: 0;
  }

  .welcome-message {
    background-color: #e8f5e9;
    border-left: 4px solid #4caf50;
    padding: 1.5rem;
    margin: 2rem 0;
    border-radius: 4px;
    text-align: left;
  }

  .welcome-message p {
    margin: 0.5rem 0;
    color: #2e7d32;
    font-size: 1.1rem;
  }

  .welcome-message strong {
    font-weight: 700;
    color: #1b5e20;
  }

  .account-details-box {
    background-color: #f5f5f5;
    border: 1px solid #ddd;
    border-radius: 8px;
    padding: 2rem;
    margin: 2rem 0;
    text-align: left;
  }

  .account-details-box h2 {
    font-size: 1.2rem;
    color: #333;
    margin-top: 0;
    margin-bottom: 1.5rem;
    text-align: center;
  }

  .detail-item {
    display: flex;
    justify-content: space-between;
    padding: 0.75rem 0;
    border-bottom: 1px solid #eee;
  }

  .detail-item:last-child {
    border-bottom: none;
  }

  .detail-item .label {
    font-weight: 600;
    color: #555;
  }

  .detail-item .value {
    color: #0066cc;
    font-weight: 500;
    word-break: break-all;
  }

  .features-box {
    background-color: #e3f2fd;
    border-left: 4px solid #2196f3;
    padding: 2rem;
    margin: 2rem 0;
    border-radius: 4px;
    text-align: left;
  }

  .features-box h2 {
    font-size: 1.2rem;
    color: #1565c0;
    margin-top: 0;
    margin-bottom: 1.5rem;
  }

  .features-box ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .features-box li {
    padding: 0.75rem 0;
    color: #333;
    font-size: 1rem;
  }

  .features-box li::before {
    content: "✓ ";
    color: #4caf50;
    font-weight: bold;
    margin-right: 0.5rem;
  }

  .action-buttons {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin: 2rem 0;
  }

  .btn {
    padding: 1rem;
    font-size: 1rem;
    border: none;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
    cursor: pointer;
    display: inline-block;
  }

  .btn-primary {
    background-color: #0066cc;
    color: white;
  }

  .btn-primary:hover {
    background-color: #0052a3;
    box-shadow: 0 4px 12px rgba(0, 102, 204, 0.3);
    transform: translateY(-2px);
  }

  .btn-secondary {
    background-color: #f5f5f5;
    color: #0066cc;
    border: 2px solid #0066cc;
  }

  .btn-secondary:hover {
    background-color: #0066cc;
    color: white;
    box-shadow: 0 4px 12px rgba(0, 102, 204, 0.2);
  }

  .redirect-message {
    color: #666;
    font-size: 0.95rem;
    margin-top: 2rem;
    line-height: 1.6;
  }

  .redirect-message a {
    color: #0066cc;
    text-decoration: none;
    font-weight: 600;
  }

  .redirect-message a:hover {
    text-decoration: underline;
  }

  .redirect-message small {
    display: block;
    margin-top: 0.5rem;
    color: #999;
  }

  #countdown {
    font-weight: bold;
    color: #0066cc;
    font-size: 1.2em;
  }

  @media (max-width: 600px) {
    .success-container {
      padding: 1rem;
    }

    .success-card {
      padding: 2rem 1.5rem;
    }

    .success-card h1 {
      font-size: 1.5rem;
    }

    .success-icon {
      font-size: 60px;
    }

    .account-details-box,
    .features-box {
      padding: 1.5rem;
    }

    .action-buttons {
      gap: 0.75rem;
    }

    .btn {
      padding: 0.75rem;
      font-size: 0.95rem;
    }

    .congratulations-card {
      padding: 2rem 1.5rem;
    }

    .modal-header h1 {
      font-size: 1.8rem;
    }

    .confetti {
      font-size: 40px;
    }

    .check-icon {
      font-size: 60px;
    }
  }
</style>

<script>
  // Load user data from localStorage and display
  document.addEventListener('DOMContentLoaded', function() {
    console.log('Success page loaded') // Debug log
    
    try {
      const userDataStr = localStorage.getItem('userData')
      console.log('User data found:', !!userDataStr) // Debug log
      
      if (userDataStr) {
        const userData = JSON.parse(userDataStr)
        const username = userData.username || 'User'
        console.log('Username:', username) // Debug log
        
        // Update congratulations modal
        const welcomeEl = document.getElementById('welcomeName')
        if (welcomeEl) {
          welcomeEl.textContent = `Welcome, ${username}!`
        }
        
        // Update success page
        const userDisplayEl = document.getElementById('userDisplay')
        if (userDisplayEl) {
          userDisplayEl.textContent = username
        }
        const usernameEl = document.getElementById('displayUsername')
        if (usernameEl) {
          usernameEl.textContent = userData.username || '---'
        }
        const emailEl = document.getElementById('displayEmail')
        if (emailEl) {
          emailEl.textContent = userData.email || '---'
        }
      }
    } catch (error) {
      console.error('Error loading user data:', error)
    }

    // Handle continue button
    const continueBtn = document.getElementById('continueBtn')
    if (continueBtn) {
      continueBtn.addEventListener('click', function() {
        const modal = document.getElementById('congratsModal')
        const content = document.getElementById('successContent')
        
        // Fade out modal
        modal.style.animation = 'fadeOut 0.3s ease-out'
        setTimeout(() => {
          modal.style.display = 'none'
          if (content) {
            content.style.display = 'block'
          }
          startCountdown()
        }, 300)
      })
    }

    // Auto-redirect from modal after 10 seconds
    let autoRedirectCount = 10
    const autoRedirectEl = document.getElementById('autoRedirect')
    if (autoRedirectEl) {
      const autoRedirectInterval = setInterval(() => {
        autoRedirectCount--
        autoRedirectEl.textContent = autoRedirectCount
        
        if (autoRedirectCount <= 0) {
          clearInterval(autoRedirectInterval)
          if (continueBtn) {
            continueBtn.click()
          }
        }
      }, 1000)
    }

    // Countdown on success page
    function startCountdown() {
      let countdown = 5
      const countdownEl = document.getElementById('countdown')

      if (countdownEl) {
        const countdownInterval = setInterval(() => {
          countdown--
          countdownEl.textContent = countdown

          if (countdown <= 0) {
            clearInterval(countdownInterval)
            window.location.href = '/'
          }
        }, 1000)
      }
    }
  })

  // Add fadeOut animation if not already in stylesheet
  if (!document.querySelector('style[data-fadeout]')) {
    const style = document.createElement('style')
    style.setAttribute('data-fadeout', 'true')
    style.textContent = `
      @keyframes fadeOut {
        from {
          opacity: 1;
        }
        to {
          opacity: 0;
        }
      }
    `
    document.head.appendChild(style)
  }
</script>
