# =========================================
# Environment Variables Configuration
# Copy this to .env and fill in your values
# NEVER commit .env file to git!
# =========================================

# Database Configuration
# Format: postgres://username:password@host:port/database_name
DATABASE_URL=postgres://cse340br:123456@localhost:5432/cse340

# Server Configuration
PORT=5500
NODE_ENV=development

# Session Security
# Generate a strong random string for production
SESSION_SECRET=your_super_secret_session_key_change_this_in_production_minimum_32_characters

# JWT Token Configuration
# Generate a strong random string for production
# This is used to sign and verify JWT tokens
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_minimum_32_characters_recommended_64

# =========================================
# Security Notes
# =========================================
# 
# 1. SESSION_SECRET
#    - Used by express-session to sign session cookies
#    - Keep it private and strong
#    - At least 32 characters recommended
#    - Rotate periodically in production
#
# 2. JWT_SECRET
#    - Used to sign JWT tokens for API authentication
#    - Keep it private and strong
#    - At least 32 characters recommended
#    - Never expose in logs or version control
#    - Consider using different secrets for dev, staging, production
#
# 3. DATABASE_URL
#    - Connection string for PostgreSQL database
#    - Format: postgres://username:password@host:port/database
#    - Use environment-specific databases for dev/prod
#    - Ensure password is secure and rotated regularly
#
# 4. NODE_ENV
#    - Set to 'development' for development
#    - Set to 'production' for production deployments
#    - Affects error messages, logging, and security features
#
# =========================================
# How to Generate Strong Secret Keys
# =========================================
#
# Option 1: Using Node.js
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Option 2: Using OpenSSL
#   openssl rand -hex 32
#
# Option 3: Using pwgen (if installed)
#   pwgen -sy 32 1
#
# =========================================
